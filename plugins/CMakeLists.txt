if(IOS)
    if(KRITA_IOS_PLUGIN_KRA)
        add_subdirectory(impex/libkra)
        add_subdirectory(impex/kra)
    endif()
    if(KRITA_IOS_PLUGIN_PNG)
        add_subdirectory(impex/png)
    endif()
    if(KRITA_IOS_PLUGIN_JPEG)
        if(JPEG_FOUND AND HAVE_LCMS2)
            add_subdirectory(impex/jpeg)
        else()
            message(FATAL_ERROR "KRITA_IOS_PLUGIN_JPEG requires JPEG and LittleCMS2")
        endif()
    endif()
    if(KRITA_IOS_PLUGIN_ORA)
        add_subdirectory(impex/ora)
    endif()
    if(KRITA_IOS_PLUGIN_EXTENDED_IMPEX)
        # These formats are part of the Android build and use only Krita/Qt
        # components that are already fixed in the iOS profile.
        add_subdirectory(impex/csv)
        add_subdirectory(impex/svg)
        add_subdirectory(impex/qimageio)
        add_subdirectory(impex/xcf)
        add_subdirectory(impex/psd)
        add_subdirectory(impex/qml)
        add_subdirectory(impex/tga)
        add_subdirectory(impex/heightmap)
        add_subdirectory(impex/brush)
        add_subdirectory(impex/spriter)
        add_subdirectory(impex/krz)
        add_subdirectory(impex/rgbe)
    endif()
    if(KRITA_IOS_PLUGIN_DEFAULT_PAINTOPS)
        add_subdirectory(paintops)
    endif()
    if(KRITA_IOS_PLUGIN_BASIC_TOOLS)
        add_subdirectory(assistants)
        add_subdirectory(tools)
    endif()
    if(KRITA_IOS_PLUGIN_LAYER_DOCKER)
        add_subdirectory(dockers)
    endif()
    if(KRITA_IOS_PLUGIN_FILTERS)
        add_subdirectory(filters)
    endif()
    if(KRITA_IOS_PLUGIN_GENERATORS)
        add_subdirectory(generators)
    endif()
    if(KRITA_IOS_PLUGIN_LCMS_ENGINE)
        add_subdirectory(color)
    endif()
    if(KRITA_IOS_PLUGIN_FLAKE)
        add_subdirectory(flake)
    endif()
    if(KRITA_IOS_PLUGIN_METADATA)
        add_subdirectory(metadata)
    endif()
    if(KRITA_IOS_PLUGIN_EXTENSIONS)
        add_subdirectory(extensions)
    endif()
    return()
endif()

add_subdirectory( assistants )
add_subdirectory( color )
add_subdirectory( dockers )
add_subdirectory( extensions )
add_subdirectory( filters )
add_subdirectory( flake )
add_subdirectory( generators )
add_subdirectory( impex )
add_subdirectory( metadata )
add_subdirectory( paintops )
add_subdirectory( tools )
add_subdirectory( qt )

if ((HAVE_PYQT5 OR HAVE_PYQT6) AND HAVE_SIP AND HAVE_PYTHONLIBS)
    add_subdirectory( python )
endif()

# This somehow breaks packaging the Qt platform plugin on Android
if (NOT ANDROID)
    add_subdirectory( platforms )
endif()
